import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Create = () => {
  return (
    <SafeAreaView>
      <View className="">
        <Text className="text-red-500 text-3xl">Create</Text>
      </View>
    </SafeAreaView>
  )
}

export default Create
