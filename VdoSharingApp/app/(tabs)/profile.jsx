import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Profile = () => {
  return (
    <SafeAreaView>
      <View className="">
        <Text className="text-red-500 text-3xl">Profile</Text>
      </View>
    </SafeAreaView>
  )
}

export default Profile
